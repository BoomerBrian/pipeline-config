---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: opetroykinsella/concourse-fly-resource
    tag: 'latest'

inputs:
   - name: pipeline-config
   
run:
  path: sh
  args: ["configure-pipelines.sh"]
